//Use JSHint or JSFiddle to check for errors
//Always declare local variables before using them.  Local variables have priority over Global variables in functions
//If a variable is initialized(assigned a value) without first being declared with the var keyword, it is automatically added to the Global context and becomes a global variable.